<% if user_signed_in? %>
<header class="row">
 <div class="naver col-left">
    <a class="navbar-brand " href="/">
      <%= image_tag '/images/logo.png' %>
    </a>
  </div>

  <div class="col-right">
    <div class="container">
      <div class="namer"><%= namer(yield(:name)) %></div>    
    </div>
  </div>
</header>
<% end %>