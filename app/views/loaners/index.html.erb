<p id="notice"><%= notice %></p>

<h1> Welcome to your Dashboard, <%= current_user.profile.firstname %>! </h1>

<h2> Here is your loan history: </h2>
<table class="showcase">
  <thead>
    <tr>
      <th>Borrowed amonunt (=N=) </th>
      <th>Duration (Days) </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @loaners.each do |loaner| %>
      <tr>
        <td><%= number_to_currency(loaner.amount, :unit => "NGN") %></td>
        <td><%= loaner.duration %></td>
        <td><%= link_to 'View Details', loaner %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<div class="btn-box">
  <%= link_to 'New Loan', new_loaner_path %>
</div>
